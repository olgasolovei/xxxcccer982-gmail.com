| № | Опис | Пріоритет | Джерело (зацікавлена особа) |
|----|-------|------------|------------------------------|
| **FR1.1** | Система повинна приймати інциденти від зовнішніх конекторів через REST API (`POST /api/v1/incidents`) з обов’язковими полями: `siteId`, `zoneId`, `timestamp`, `confidence`, `sourceId`, `evidenceRef`. | High | Product Owner (Ірина), Системний архітектор (Микола) |
| **FR1.1.1** | Правило присвоєння `type` за значенням `confidence` (float): якщо `confidence` >= 5 і < 7 → `FALL_RISK`; якщо `confidence` >= 7 і <= 10 → `FALL`. Значення `confidence` < 5 зберігаються як `INFO` або `POTENTIAL` (без створення alert). | High | Product Owner (Ірина) |
| **FR1.1.2** | Система повинна автоматично створювати запис `Alert` лише для інцидентів типу `FALL`. | High | Product Owner (Ірина), Stakeholder (Олександр) |
| **FR1.2** | Система повинна зберігати інциденти зі статусом `NEW` одразу після прийому даних. | High | Product Owner (Ірина) |
| **FR1.3** | Система повинна забезпечувати можливість подальшої ескалації інциденту (логічна можливість розширення, без реалізації черг у спрощеній версії). | Medium | Stakeholder (Олександр) |
| **FR1.5** | Система має зберігати значення `confidence` як числове поле у записі інциденту для подальшого аналізу та звітності. | Medium | Product Owner (Ірина) |
| **FR2.1** | Адміністратор повинен мати можливість зареєструвати новий будівельний майданчик (`Site`) із зазначенням назви та локації через UI або API. | High | Product Owner (Ірина), Керівник проєкту (Андрій) |
| **FR2.2** | Система повинна дозволяти створювати зони (`Zone`) в межах конкретного `Site`. | High | Розробник |
| **FR2.3** | Адміністратор має мати можливість зареєструвати новий IoT-пристрій (`Device`) із полями `sourceId`, `type`, `siteId`, `metadata` без використання механізмів автентифікації в спрощеній логіці. | High | Системний архітектор (Микола), Розробник (Олег) |
| **FR3.1** | Система повинна надавати можливість перегляду списку інцидентів за `siteId` та часовим періодом через UI. | High | Керівник будівельного майданчика (Василь) |
| **FR3.2** | Система повинна генерувати звіт з безпеки за вибраний період у форматі CSV або JSON. | Medium | Product Owner (Ірина), Stakeholder (Олександр) |
| **FR4.1** | Система повинна надавати веб-інтерфейс для створення та перегляду `Site`, `Zone`, `Device` та `Incident`. | Medium | Product Owner (Ірина) |
| **FR4.2** | Веб-інтерфейс повинен відображати актуальний статус та тип інциденту (INFO / FALL_RISK / FALL) у вигляді зрозумілих бейджів. | Medium | Інженер з техніки безпеки (Сергій) |

